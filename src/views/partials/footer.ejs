    </main>
    <footer class="site-footer">
      <div class="footer-inner">
        <span>Built for local use on the church network.</span>
        <% if (lanUrl) { %>
          <span>Open <a href="<%= lanUrl %>"><%= lanUrl %></a> on your device's browser.</span>
        <% } else { %>
          <span>Open this app from another device on the same trusted LAN.</span>
        <% } %>
      </div>
    </footer>
  </div>
</body>
</html>
